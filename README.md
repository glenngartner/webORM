# web-based ORM built with TypeORM
        
To run, clone this repository, cd into it, then type these commands in your terminal:

```bash
npm i
``` 

```bash
npm start
```

You'll see a console output
